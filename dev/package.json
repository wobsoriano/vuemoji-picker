{
  "name": "playground",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "deploy": "pnpm run build && gh-pages -d dist",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "vuemoji-picker": "workspace:*"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "@vue/compiler-sfc": "^3.3.4",
    "gh-pages": "^5.0.0",
    "typescript": "^5.1.6",
    "vite": "^3.2.7",
    "vue-tsc": "^1.8.4"
  },
  "homepage": "https://wobsoriano.github.io/vuemoji-picker"
}
